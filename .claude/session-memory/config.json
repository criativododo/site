{
  "schemaVersion": 1,
  "memoryRepositoryUrl": "https://github.com/criativododo/criativododo-memory.git",
  "memoryDirectory": "criativododo-memory",
  "journalWindow": 5,
  "checks": {
    "app": ["npm run lint", "npm run build"],
    "portal-frontend": ["npm run lint", "npm run build"],
    "portal-backend": ["npm run typecheck", "npm test", "npm run build"]
  }
}
