export function PendenciasPage() {
  return (
    <section>
      <h1 className="title-editorial" style={{ fontSize: 28, marginBottom: 16 }}>
        Pendências do mês
      </h1>
      <p style={{ fontSize: 15 }}>
        Módulo de Conteúdo (SPEC-027, EPIC 2) — a implementar. Esta tela nasce como rota
        protegida do Portal (EPIC 1), sem dado real ainda.
      </p>
    </section>
  );
}
